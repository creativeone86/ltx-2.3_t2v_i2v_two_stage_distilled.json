# ltx-2.3_t2v_i2v_two_stage_distilled.json

Dockerized ComfyUI workflow: LTX-2.3_T2V_I2V_Two_Stage_Distilled.json

## Contents

- `Dockerfile` - Docker container configuration for running this ComfyUI workflow
- `example-request.json` - Example API request payload for testing

## Usage

```bash
# Build the Docker image
docker build -t ltx-2.3_t2v_i2v_two_stage_distilled.json .

# Run the container
docker run -p 8188:8188 ltx-2.3_t2v_i2v_two_stage_distilled.json
```

## API Request Example

See `example-request.json` for a ready-to-use API request payload.

Generated by [ComfyUI to Docker Wizard](undefined)
